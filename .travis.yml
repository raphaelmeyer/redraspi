sudo: false

language: python

python:
  - "3.6"

before_install:
  - git clone https://github.com/pimoroni/flotilla-python

install:
  - pip install behave
  - pip install python-twitter
  - pushd flotilla-python/library && ./setup.py install && popd

script:
  - nosetests
  - behave -t ~@wip

